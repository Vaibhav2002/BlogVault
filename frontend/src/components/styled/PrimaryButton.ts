import {Button, styled} from "@mui/material";

const PrimaryButton = styled(Button)(() => ({
    borderRadius: '8px',
    textTransform: 'none',
    padding: '0.7rem'
}))

export default PrimaryButton