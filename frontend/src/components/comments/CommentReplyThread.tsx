import React from 'react';
import Comment from "@/data/models/Comment";

interface CommentReplyThreadProps {
    comment: Comment
    className?: string
}

const CommentReplyThread = ({className}: CommentReplyThreadProps) => {

    return (
        <></>
    )
}

export default CommentReplyThread;
